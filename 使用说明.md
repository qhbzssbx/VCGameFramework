# 流程系统使用指南

## 📋 概述

这个流程系统提供了一个完整的游戏状态管理解决方案，支持主流程切换和子流程栈管理。

## 🚀 快速开始

### 1. 项目集成

#### 步骤1：配置VContainer模块

```csharp
// 在你的项目启动类中注册模块
public class ProjectBootstrap : LifetimeScope
{
    protected override void Configure(IContainerBuilder builder)
    {
        // 注册核心流程系统
        builder.RegisterModule<FlowSystemModule>();
        
        // 注册具体的游戏流程
        builder.RegisterModule<GameFlowModule>();
    }
}
```

#### 步骤2：创建游戏启动脚本

```csharp
public class GameBootstrap : MonoBehaviour
{
    [Inject] private IFlowManager _flowManager;
    
    private async void Start()
    {
        // 启动第一个流程
        await _flowManager.SwitchToFlow<LaunchFlow>();
    }
}
```

### 2. 基本使用

#### 主流程切换

```csharp
// 注入流程管理器
[Inject] private IFlowManager _flowManager;

// 切换到不同的主流程
await _flowManager.SwitchToFlow<LaunchFlow>();      // 启动流程
await _flowManager.SwitchToFlow<HotUpdateFlow>();   // 热更新流程
await _flowManager.SwitchToFlow<LoginFlow>();       // 登录流程
await _flowManager.SwitchToFlow<GameMainFlow>();    // 游戏主流程
```

#### 子流程管理

```csharp
// 注入子流程管理器
[Inject] private ISubFlowManager _subFlowManager;

// 打开子流程
await _subFlowManager.PushSubFlow<PauseMenuSubFlow>();

// 带数据的子流程
var context = FlowContextBuilder.Create()
    .WithData("FromFlow", "GameMain")
    .WithData("InitialTab", "Equipment")
    .Build();
await _subFlowManager.PushSubFlow<InventorySubFlow>(context);

// 关闭当前子流程
await _subFlowManager.PopSubFlow();

// 关闭所有子流程
await _subFlowManager.PopToRoot();
```

## 📚 详细功能

### 数据传递

使用 `FlowContext` 在流程间传递数据：

```csharp
// 创建数据上下文
var context = FlowContextBuilder.Create()
    .WithData("PlayerLevel", 25)
    .WithData("CurrentMap", "Forest")
    .WithData("GameMode", "Adventure")
    .Build();

// 在流程中使用数据
protected override async UniTask OnEnterInternal(FlowContext context)
{
    var playerLevel = context?.Get<int>("PlayerLevel") ?? 1;
    var currentMap = context?.Get<string>("CurrentMap") ?? "Default";
    
    // 使用数据...
}
```

### 事件系统

监听流程事件：

```csharp
[Inject] private IFlowEventPublisher _eventPublisher;

private void Start()
{
    // 订阅流程进入事件
    _eventPublisher.Subscribe<FlowEnterEvent>(OnFlowEnter);
    
    // 订阅流程退出事件
    _eventPublisher.Subscribe<FlowExitEvent>(OnFlowExit);
}

private void OnFlowEnter(FlowEnterEvent evt)
{
    Debug.Log($"流程进入: {evt.FlowName}");
}
```

### 管理器使用

#### 时间管理器

```csharp
[Inject] private ITimeManager _timeManager;

// 暂停游戏
_timeManager.PauseGame();

// 恢复游戏
_timeManager.ResumeGame();

// 设置时间缩放
_timeManager.PushTimeScale(0.5f);  // 慢动作
_timeManager.PopTimeScale();       // 恢复
```

#### 音频管理器

```csharp
[Inject] private IAudioManager _audioManager;

// 播放音效
_audioManager.PlaySFX(audioClip);

// 控制音量
_audioManager.MasterVolume = 0.8f;
_audioManager.SFXVolume = 0.6f;

// 静音控制
_audioManager.SetSFXMuted(true);
```

#### 输入管理器

```csharp
[Inject] private IInputManager _inputManager;

// 设置输入模式
_inputManager.SetUIOnlyMode();    // 只允许UI输入
_inputManager.EnableInput();      // 启用所有输入
_inputManager.DisableInput();     // 禁用所有输入

// 监听输入事件
_inputManager.OnBackPressed += OnBackPressed;
_inputManager.OnConfirmPressed += OnConfirmPressed;
```

## 🎯 内置流程说明

### 主流程

1. **LaunchFlow** - 启动流程
   - 初始化基础系统
   - 加载必要资源
   - 自动跳转到热更新流程

2. **HotUpdateFlow** - 热更新流程
   - 检查更新信息
   - 下载更新内容
   - 验证更新完整性

3. **LoginFlow** - 登录流程
   - 用户身份验证
   - 加载用户数据
   - 连接游戏服务器

4. **GameMainFlow** - 游戏主流程
   - 游戏核心逻辑管理
   - 场景加载和切换
   - 游戏状态维护

### 子流程

1. **GamePlaySubFlow** - 游戏进行中
   - 不暂停父流程
   - 显示游戏HUD
   - 处理游戏逻辑更新

2. **PauseMenuSubFlow** - 暂停菜单
   - 暂停父流程
   - 暂停游戏时间和音效
   - 提供继续/设置/退出选项

3. **SettingsSubFlow** - 设置菜单
   - 暂停父流程
   - 完整的设置管理
   - 支持音频/图形/控制等设置

4. **InventorySubFlow** - 背包界面
   - 不暂停父流程
   - 物品分类管理
   - 支持使用/丢弃物品

## 💡 最佳实践

### 1. 流程设计原则

- **单一职责**：每个流程只负责一个明确的功能
- **松耦合**：流程间通过事件和数据上下文通信
- **可扩展**：易于添加新的流程而不影响现有代码

### 2. 错误处理

```csharp
try
{
    await _flowManager.SwitchToFlow<GameMainFlow>();
}
catch (FlowException ex)
{
    Debug.LogError($"流程切换失败: {ex.Message}");
    // 处理错误，可能回到安全的流程
    await _flowManager.SwitchToFlow<LaunchFlow>();
}
```

### 3. 性能考虑

- 流程切换是异步操作，避免频繁切换
- 子流程使用栈管理，注意避免过深的嵌套
- 及时清理不需要的事件订阅

### 4. 调试技巧

- 使用 `Debug.Log` 输出流程状态变化
- 监听流程事件来跟踪系统行为
- 在Inspector中查看当前活动的流程

## 🔧 自定义扩展

### 创建自定义主流程

```csharp
public class CustomMainFlow : BaseMainFlow
{
    public override string FlowName => "自定义主流程";
    
    protected override async UniTask OnEnterInternal(FlowContext context)
    {
        // 进入逻辑
    }
    
    protected override async UniTask OnUpdateInternal()
    {
        // 更新逻辑
    }
    
    protected override async UniTask OnExitInternal()
    {
        // 退出逻辑
    }
}
```

### 创建自定义子流程

```csharp
public class CustomSubFlow : BaseSubFlow
{
    public override string FlowName => "自定义子流程";
    public override bool ShouldPauseParent => true; // 是否暂停父流程
    
    protected override async UniTask OnEnterInternal(FlowContext context)
    {
        // 进入逻辑
    }
    
    protected override async UniTask OnExitInternal()
    {
        // 退出逻辑
    }
}
```

### 注册自定义流程

```csharp
public class CustomFlowModule : IModuleWithOrder
{
    public int Order => -50;
    
    public void Configure(IContainerBuilder builder)
    {
        // 注册自定义主流程
        FlowSystemModule.RegisterFlow<CustomMainFlow>(builder);
        
        // 注册自定义子流程
        FlowSystemModule.RegisterSubFlow<CustomSubFlow>(builder);
    }
}
```

## 🐛 常见问题

### Q: 如何在流程间传递复杂数据？

A: 使用 FlowContext 传递数据，支持任何类型：

```csharp
var context = FlowContextBuilder.Create()
    .WithData("UserProfile", userProfileObject)
    .WithData("GameSettings", settingsObject)
    .Build();
```

### Q: 子流程可以无限嵌套吗？

A: 理论上可以，但建议控制在3-4层以内，避免过深的嵌套影响性能和用户体验。

### Q: 如何处理流程切换失败？

A: 使用 try-catch 捕获异常，并提供回退机制：

```csharp
try
{
    await _flowManager.SwitchToFlow<TargetFlow>();
}
catch (Exception ex)
{
    Debug.LogError($"流程切换失败: {ex.Message}");
    await _flowManager.SwitchToFlow<SafeFlow>(); // 回退到安全流程
}
```

---

🎉 现在你已经掌握了流程系统的使用方法！开始构建你的游戏逻辑吧！